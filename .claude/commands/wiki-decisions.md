Walk the last 10 commits and decide: did any of these changes pin a decision that overturns or replaces a previous approach?

Signals:
- a dependency removed and an alternative added
- one implementation of a module swapped for another
- a configuration change not driven by a bug

If found, propose an entry for `.wiki/DECISIONS.md` matching the format of the existing entries. Don't write it without confirmation.
