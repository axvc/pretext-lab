Check the wiki for integrity:

1. Module / component names mentioned in `.wiki/ARCHITECTURE.md` — do they all still exist in the codebase?
2. Linear issue IDs in `.wiki/STATE.md` — are they all still active (not closed / cancelled)?
3. Contradictions between `STATE.md` and `DECISIONS.md` (e.g. STATE mentions an approach DECISIONS already reversed).
4. "Open questions" in `STATE.md` — any that have actually been resolved and left there?

Print a report. Don't edit without confirmation.
