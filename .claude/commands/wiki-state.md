Read:
- `git log` for the last 7 days
- the current `.wiki/STATE.md`
- recent updates in the active Linear project (via Linear MCP / API, if available)

Update `.wiki/STATE.md`:
- Move completed items from "In progress" to "Done (recent)"
- Add new tasks to "In progress" / "On the horizon"
- Update the `Last updated` date

Show the diff before writing.
