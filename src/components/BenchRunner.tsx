export function BenchRunner() {
  return (
    <div
      style={{
        border: "1px dashed var(--color-border)",
        borderRadius: 6,
        padding: "1.5rem",
        margin: "1.5rem 0",
        fontFamily: "var(--font-mono)",
        fontSize: "0.875rem",
        color: "var(--color-muted)",
      }}
    >
      <div style={{ color: "var(--color-accent)", marginBottom: "0.5rem" }}>
        BenchRunner placeholder
      </div>
      <div>Not implemented yet — wired only to verify the React-island pipeline.</div>
    </div>
  );
}
